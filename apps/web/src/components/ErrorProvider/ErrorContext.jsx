import { createContext } from "react";
const noOp = () => {
    // Default no-op function for context
};
export const ErrorContext = createContext({
    showError: noOp
});
//# sourceMappingURL=ErrorContext.jsx.map