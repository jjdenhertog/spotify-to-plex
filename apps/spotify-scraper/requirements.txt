# Flask web framework
Flask==2.3.2
flask-cors==4.0.0

# Spotify scraping library
# Pinned: installing from git HEAD meant every rebuild could pull a breaking
# release. 3.9.x renamed the playlist/track methods, which silently broke
# curated-playlist imports on rebuild. Bump this deliberately, never implicitly.
spotifyscraper==3.9.2

# HTTP client and utilities
requests==2.31.0

# Environment and configuration
python-dotenv==1.0.0

# Production server
gunicorn==21.2.0